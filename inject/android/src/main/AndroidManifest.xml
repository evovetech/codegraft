<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="codegraft.inject.android">

    <application>
        <provider
            android:authorities="${applicationId}.BootstrapProvider"
            android:name=".BootstrapProvider"
            android:exported="false"
            android:multiprocess="true"
            android:initOrder="0x7fffffff" />
    </application>
</manifest>
