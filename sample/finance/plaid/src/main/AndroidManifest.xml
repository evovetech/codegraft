<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="evovetech.finance.plaid">

    <application>
        <activity android:name=".PlaidActivity" />
        <provider
            android:authorities="${applicationId}.PlaidProvider"
            android:name=".PlaidProvider"
            android:exported="false" />
        <receiver android:name=".PlaidReceiver" />
        <service android:name=".PlaidService" />
    </application>

</manifest>
